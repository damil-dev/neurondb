# NeuronAgent Docker Environment Configuration
# Copy this file to .env and customize for your environment
# 
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up -d

# =============================================================================
# Database Connection
# =============================================================================

# Database hostname
# - For host machine: localhost
# - For Docker network: neurondb-cpu (container name)
# - For Docker Desktop (Mac/Windows): host.docker.internal
# - For Linux: use Docker network or localhost with port mapping
DB_HOST=localhost

# Database port
# - For host machine: 5433 (default Docker port mapping)
# - For Docker network: 5432 (internal container port)
DB_PORT=5433

# Database name
DB_NAME=neurondb

# Database username
DB_USER=neurondb

# Database password
# WARNING: For production, use Docker secrets or external secrets management
DB_PASSWORD=neurondb

# Database connection pool settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m

# =============================================================================
# Server Configuration
# =============================================================================

# Server bind address (0.0.0.0 to listen on all interfaces)
SERVER_HOST=0.0.0.0

# Server port
SERVER_PORT=8080

# Server timeouts
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s

# =============================================================================
# Authentication
# =============================================================================

# API key header name (default: X-API-Key)
# AUTH_API_KEY_HEADER=X-API-Key

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level: debug, info, warn, error
LOG_LEVEL=info

# Log format: json, text
LOG_FORMAT=json

# =============================================================================
# Configuration File
# =============================================================================

# Optional: Path to config.yaml file (inside container)
# CONFIG_PATH=/app/config.yaml

# =============================================================================
# SSL/TLS Configuration (Optional)
# =============================================================================

# SSL mode for database connection
# Options: disable, allow, prefer, require, verify-ca, verify-full
# DB_SSL_MODE=require

# Path to SSL certificate (inside container, if using verify-ca/verify-full)
# DB_SSL_CERT=/app/ssl/client-cert.pem
# DB_SSL_KEY=/app/ssl/client-key.pem
# DB_SSL_ROOT_CERT=/app/ssl/ca-cert.pem

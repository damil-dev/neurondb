# NeuronMCP Docker Environment Configuration
# Copy this file to .env and customize for your environment
# 
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up -d

# =============================================================================
# Database Connection
# =============================================================================

# Database hostname
# - For host machine: localhost
# - For Docker network: neurondb-cpu (container name)
# - For Docker Desktop (Mac/Windows): host.docker.internal
# - For Linux: use Docker network or localhost with port mapping
NEURONDB_HOST=localhost

# Database port
# - For host machine: 5433 (default Docker port mapping)
# - For Docker network: 5432 (internal container port)
NEURONDB_PORT=5433

# Database name
NEURONDB_DATABASE=neurondb

# Database username
NEURONDB_USER=neurondb

# Database password
# WARNING: For production, use Docker secrets or external secrets management
NEURONDB_PASSWORD=neurondb

# Alternative: Full connection string (overrides above settings)
# Format: postgresql://user:password@host:port/database?sslmode=require
# NEURONDB_CONNECTION_STRING=

# =============================================================================
# Configuration File
# =============================================================================

# Path to MCP configuration file (inside container)
# Default: /app/mcp-config.json
# To use mounted config: /app/mcp-config.json
NEURONDB_MCP_CONFIG=/app/mcp-config.json

# =============================================================================
# Logging Configuration
# =============================================================================

# Log level: debug, info, warn, error
NEURONDB_LOG_LEVEL=info

# Log format: json, text
NEURONDB_LOG_FORMAT=text

# Log output: stdout, stderr, file
# For Docker, stderr is recommended for proper log collection
NEURONDB_LOG_OUTPUT=stderr

# =============================================================================
# Feature Flags
# =============================================================================

# Enable GPU acceleration (requires GPU-enabled NeuronDB instance)
NEURONDB_ENABLE_GPU=false

# =============================================================================
# SSL/TLS Configuration (Optional)
# =============================================================================

# SSL mode for database connection
# Options: disable, allow, prefer, require, verify-ca, verify-full
# NEURONDB_SSL_MODE=require

# Path to SSL certificate (inside container, if using verify-ca/verify-full)
# NEURONDB_SSL_CERT=/app/ssl/client-cert.pem
# NEURONDB_SSL_KEY=/app/ssl/client-key.pem
# NEURONDB_SSL_ROOT_CERT=/app/ssl/ca-cert.pem

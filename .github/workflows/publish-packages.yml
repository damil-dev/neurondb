name: Publish Packages to Repository (DEPRECATED)

# This workflow is deprecated. Packaging infrastructure has been removed
# in favor of Docker-based deployment. Use publish-containers.yml instead.
#
# To re-enable package publishing, restore the packaging/ directory structure
# from git history and uncomment the triggers below.

on:
  # Disabled - uncomment to re-enable
  # workflow_dispatch:
  #   inputs:
  #     version:
  #       description: 'Package version (e.g., 1.0.0)'
  #       required: false
  #       type: string

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deprecated workflow notice
      run: |
        echo "⚠️  This workflow is deprecated." >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "The packaging infrastructure has been removed in favor of Docker-based deployment." >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "Please use the \`publish-containers.yml\` workflow to publish container images instead." >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "To restore package publishing, restore the \`packaging/\` directory from git history." >> $GITHUB_STEP_SUMMARY
        exit 1


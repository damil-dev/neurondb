{{- if .Values.neuronagent.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "neurondb.fullname" . }}-neuronagent
  labels:
    {{- include "neurondb.neuronagent.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.neuronagent.replicas }}
  selector:
    matchLabels:
      {{- include "neurondb.neuronagent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "neurondb.neuronagent.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "neurondb.neuronagent.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.neuronagent.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.neuronagent.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.neuronagent.affinity }}
      affinity:
        {{- toYaml .Values.neuronagent.affinity | nindent 8 }}
      {{- end }}
      {{- if .Values.neuronagent.tolerations }}
      tolerations:
        {{- toYaml .Values.neuronagent.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.neuronagent.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml .Values.neuronagent.topologySpreadConstraints | nindent 8 }}
      {{- end }}
      {{- if and .Values.resourceGovernance.priorityClass.enabled (not .Values.neuronagent.priorityClassName) }}
      priorityClassName: {{ include "neurondb.fullname" . }}-priority
      {{- else if .Values.neuronagent.priorityClassName }}
      priorityClassName: {{ .Values.neuronagent.priorityClassName }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        allowPrivilegeEscalation: false
        capabilities:
          drop:
          - ALL
        seccompProfile:
          type: RuntimeDefault
      {{- if .Values.neuronagent.initWaitForDB }}
      initContainers:
      - name: wait-for-db
        image: busybox:1.36
        command:
        - sh
        - -c
          - |
            until nc -z {{ include "neurondb.postgresHost" . }} {{ include "neurondb.postgresPort" . }}; do
              echo "Waiting for database..."
              sleep 2
            done
            echo "Database is ready!"
      {{- end }}
      containers:
      - name: neuronagent
        image: "{{ if .Values.global.imageRegistry }}{{ .Values.global.imageRegistry }}/{{ end }}{{ .Values.neuronagent.image.repository }}:{{ .Values.neuronagent.image.tag }}"
        imagePullPolicy: {{ .Values.neuronagent.image.pullPolicy }}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: {{ .Values.neuronagent.securityContext.readOnlyRootFilesystem | default false }}
          runAsNonRoot: true
          runAsUser: 1000
          seccompProfile:
            type: RuntimeDefault
        ports:
        - name: http
          containerPort: {{ .Values.neuronagent.service.port }}
          protocol: TCP
        env:
        - name: DB_HOST
          {{- if and .Values.neurondb.postgresql.external.enabled .Values.neurondb.postgresql.external.connectionString }}
          valueFrom:
            secretKeyRef:
              name: {{ include "neurondb.postgresPasswordSecret" . }}
              key: {{ .Values.neurondb.postgresql.external.secretKeys.connectionString | default "connection-string" }}
          {{- else if and .Values.neurondb.postgresql.external.enabled .Values.neurondb.postgresql.external.secretName }}
          valueFrom:
            secretKeyRef:
              name: {{ include "neurondb.postgresPasswordSecret" . }}
              key: {{ .Values.neurondb.postgresql.external.secretKeys.host | default "host" }}
          {{- else }}
          value: {{ include "neurondb.postgresHost" . }}
          {{- end }}
        - name: DB_PORT
          {{- if and .Values.neurondb.postgresql.external.enabled .Values.neurondb.postgresql.external.secretName }}
          valueFrom:
            secretKeyRef:
              name: {{ include "neurondb.postgresPasswordSecret" . }}
              key: {{ .Values.neurondb.postgresql.external.secretKeys.port | default "port" }}
          {{- else }}
          value: {{ include "neurondb.postgresPort" . | quote }}
          {{- end }}
        - name: DB_NAME
          {{- if and .Values.neurondb.postgresql.external.enabled .Values.neurondb.postgresql.external.secretName }}
          valueFrom:
            secretKeyRef:
              name: {{ include "neurondb.postgresPasswordSecret" . }}
              key: {{ .Values.neurondb.postgresql.external.secretKeys.database | default "database" }}
          {{- else }}
          value: {{ include "neurondb.postgresDatabase" . | quote }}
          {{- end }}
        - name: DB_USER
          {{- if and .Values.neurondb.postgresql.external.enabled .Values.neurondb.postgresql.external.secretName }}
          valueFrom:
            secretKeyRef:
              name: {{ include "neurondb.postgresPasswordSecret" . }}
              key: {{ .Values.neurondb.postgresql.external.secretKeys.username | default "username" }}
          {{- else }}
          value: {{ include "neurondb.postgresUsername" . | quote }}
          {{- end }}
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "neurondb.postgresPasswordSecret" . }}
              key: {{ include "neurondb.postgresPasswordKey" . }}
        {{- if and .Values.neurondb.postgresql.external.enabled .Values.neurondb.postgresql.external.connectionString }}
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: {{ include "neurondb.postgresPasswordSecret" . }}
              key: {{ .Values.neurondb.postgresql.external.secretKeys.connectionString | default "connection-string" }}
        {{- end }}
        - name: SERVER_PORT
          value: {{ .Values.neuronagent.service.port | quote }}
        - name: LOG_LEVEL
          value: {{ .Values.neuronagent.logLevel | default "info" | quote }}
        startupProbe:
          httpGet:
            path: {{ .Values.neuronagent.healthCheck.readinessProbe.path | default "/health" }}
            port: http
          initialDelaySeconds: 0
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
          successThreshold: 1
        livenessProbe:
          httpGet:
            path: {{ .Values.neuronagent.healthCheck.livenessProbe.path | default "/health" }}
            port: http
          initialDelaySeconds: {{ .Values.neuronagent.healthCheck.livenessProbe.initialDelaySeconds | default 30 }}
          periodSeconds: {{ .Values.neuronagent.healthCheck.livenessProbe.periodSeconds | default 30 }}
          timeoutSeconds: {{ .Values.neuronagent.healthCheck.livenessProbe.timeoutSeconds | default 10 }}
          failureThreshold: {{ .Values.neuronagent.healthCheck.livenessProbe.failureThreshold | default 3 }}
        readinessProbe:
          httpGet:
            path: {{ .Values.neuronagent.healthCheck.readinessProbe.path | default "/health" }}
            port: http
          initialDelaySeconds: {{ .Values.neuronagent.healthCheck.readinessProbe.initialDelaySeconds | default 10 }}
          periodSeconds: {{ .Values.neuronagent.healthCheck.readinessProbe.periodSeconds | default 10 }}
          timeoutSeconds: {{ .Values.neuronagent.healthCheck.readinessProbe.timeoutSeconds | default 5 }}
          failureThreshold: {{ .Values.neuronagent.healthCheck.readinessProbe.failureThreshold | default 3 }}
          successThreshold: {{ .Values.neuronagent.healthCheck.readinessProbe.successThreshold | default 1 }}
        resources:
          {{- toYaml .Values.neuronagent.resources | nindent 10 }}
        {{- if .Values.neuronagent.securityContext.readOnlyRootFilesystem }}
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: var-tmp
          mountPath: /var/tmp
        {{- end }}
      {{- if .Values.neuronagent.securityContext.readOnlyRootFilesystem }}
      volumes:
      - name: tmp
        emptyDir: {}
      - name: var-tmp
        emptyDir: {}
      {{- end }}
{{- end }}

